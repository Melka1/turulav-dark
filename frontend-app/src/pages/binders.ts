/**
 * Side-effect imports — each module registers its page binder.
 * Add a new line whenever a slice introduces a new bound page.
 */
import './signup';
import './login';
import './profile';
import './members';
import './groups';
